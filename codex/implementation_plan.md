# Codex Implementation Plan

## Context

We are modifying `MiSTer-devel/Amstrad_MiSTer` to add an experimental folder-backed mass-storage device for the CPC. Do not implement the full M4 board. Do not emulate STM32 or ESP8266. First goal is a minimal expansion ROM. The FPGA mailbox comes after the ROM smoke test is proven.

The Amstrad MiSTer README says the core already supports selectable expansion ROM loading and `.eXX` expansion ROM files. Use that existing mechanism for the ROM.

## Stage 1: ROM smoke test

1. Add or build a 16KB expansion ROM that registers `|HELLO`.
2. Load it as `boot.eXX` using the existing expansion ROM feature.
3. Verify `|HELLO` prints: `M4S ROM OK`.

Deliverables:

- `rom/m4s_rom.asm` made buildable.
- `make` produces `build/boot.eXX` or another valid `.eXX` file.
- Documentation explaining where to copy the ROM on MiSTer.

## Stage 2: FPGA mailbox

1. Add `rtl/m4s_mailbox.sv` to the Quartus project.
2. Instantiate it in the appropriate top-level/core module.
3. Decode experimental I/O ports:
   - `&FBD0` DATA
   - `&FBD1` STATUS
   - `&FBD2` COMMAND
   - `&FBD3` PARAM
4. Wire Z80 I/O read/write strobes to the mailbox.
5. Return mock data for `CMD_PING`, then `CMD_DIR_BEGIN`.

Acceptance:

- `|M4DIR` prints mock directory listing from FPGA-generated bytes.

## Stage 3: Host/HPS bridge investigation and preload index

Investigate current Amstrad core use of `hps_io`, `ioctl_*`, mounted disk signals, and any existing block-device plumbing. Prefer reusing current patterns before inventing new HPS extensions.

Important constraint:

- The official hps_io docs say the `EXT_BUS` is for exceptional cases and requires HPS-side MiSTer binary code. Avoid depending on custom MiSTer binary changes for the first proof-of-concept unless unavoidable.

Possible routes:

1. Use existing file loading/mounting mechanisms to preload data into memory/file buffers.
2. Use a mounted image as an intermediate backend.
3. Only later consider a proper MiSTer-side helper or extension if folder-backed enumeration truly requires it.

Chosen first route:

- Add a `Load M4S index` menu item using the existing `ioctl_download` path.
- Store the downloaded text in a small FPGA RAM buffer.
- Make `|M4DIR` stream that buffer.
- If no index has been loaded, return a sane fallback message instead of mock
  filenames.

Acceptance:

- Before loading an index, `|M4DIR` prints `NO M4S INDEX`.
- After loading a text index, `|M4DIR` prints the downloaded contents.

Second route:

- Add an Amstrad-specific Main_MiSTer helper.
- Resolve `shared_folder` using the same convention as the existing x86 and
  Minimig support.
- Periodically list that folder and push the text listing to the Amstrad core
  through a tiny write-only `EXT_BUS` protocol.
- Reuse the Stage 3A FPGA directory index buffer, so `|M4DIR` does not need a
  new CPC-side protocol yet.

Acceptance:

- With the custom Main_MiSTer binary installed, `|M4DIR` prints a live-ish
  listing of the configured shared folder without manually loading an index.

## Stage 4: Real file commands

Implement:

- `|M4DIR` returns real folder contents.
- `|CD,"path"` changes current folder.
- `|LOAD,"file"` loads a binary or BASIC file into CPC memory.
- `|SAVE,"file"` writes bytes back.

Define exact BASIC/AMSDOS semantics before promising compatibility.

## Stage 5: Polish

- Wildcards.
- File type handling.
- AMSDOS-like transparent `LOAD`, `SAVE`, `CAT`.
- Optional M4-compatible command aliases.
- Optional network later, as a separate architecture decision.
